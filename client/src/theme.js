import createTheme from "@mui/material/styles/createTheme";

const theme = createTheme({
  palette: {
    primary: {
      light: "#1976d2",
      main: "#4D81B7",
      contrastText: "#FFFFFF",
    },
  },
});

export default theme;
